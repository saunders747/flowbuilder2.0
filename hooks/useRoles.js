/**
 * useRoles.js — re-exports from AppSettings (global backend persistence).
 * Import site compatibility shim — existing imports continue to work unchanged.
 */
export { useRoles } from '@/hooks/useAppSettings';